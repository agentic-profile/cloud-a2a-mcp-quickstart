# Redis Configuration
# Copy this file to .env.local and customize for your local development environment
#
# IMPORTANT: You must install and start Redis manually on your machine
# Run ./scripts/start-local-redis.sh for installation instructions

# Redis connection settings
# For local development, leave these unset to use localhost:6379
# For production, set these to your cloud Redis endpoint
VALKEY_ENDPOINT=localhost
VALKEY_PORT=6379
VALKEY_PASSWORD=
VALKEY_DB=0

# Cloud Redis settings (only needed for production)
# VALKEY_PASSWORD_SECRET_ARN=your-secret-arn

# AWS Configuration
# AWS_REGION=us-east-1

# Local server settings
PORT=3000

# Development workflow:
# 1. Install Redis manually (see ./scripts/start-local-redis.sh)
# 2. Start Redis service
# 3. Copy this file to .env.local
# 4. Run: npm run test:local-redis
# 5. Run: npm run start:local
